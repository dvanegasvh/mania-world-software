import React from 'react';

/**
 * This interface represents the props of the layout component.
 *
 * @interface ILayoutProps
 * @property {React.ReactNode | React.ReactNode[]} children - The children of the layout.
 */
export interface ILayoutProps {
	children: React.ReactNode | React.ReactNode[];
}
